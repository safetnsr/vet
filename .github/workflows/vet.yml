name: vet
on:
  pull_request:
    branches: [main]

jobs:
  vet:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        with:
          threshold: '4.0'
